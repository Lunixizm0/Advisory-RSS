[project]
name = "advisory-rss"
version = "0.2.0"
description = "Security-first localhost-only service that lists GitHub Security Advisories created by the authenticated account as RSS 2.0 for Zen Browser"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Advisory RSS" }]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<0.140",
    "uvicorn[standard]>=0.34",
    "httpx>=0.27",
    "pydantic>=2.9",
    "pydantic-settings>=2.4",
    "python-dotenv>=1.0",
    "click>=8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "respx>=0.22",
    "httpx>=0.27",
]

[project.scripts]
app = "advisory_rss.cli:cli"
advisory-rss = "advisory_rss.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/advisory_rss"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q --strict-markers"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
